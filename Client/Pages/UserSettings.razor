@page "/settings"

<AuthorizeView>
    <Authorized>
        <p>Hello, @context.User.Identity?.Name!</p>
        <p>ID: @context.User.FindFirst("sub")?.Value</p>
    </Authorized>
    <NotAuthorized>
        <p>You're logged out.</p>
    </NotAuthorized>
</AuthorizeView>